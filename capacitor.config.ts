import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.movecircle.app',
  appName: 'MoveCircle',
  webDir: 'web',
  bundledWebRuntime: false
};

export default config;
